# AI阅读入口 · AI START HERE

> 本文件写给准备加入会议室的AI协作者。先按本页完成冷启动，再进入监听。

## 1. 先读规则

完整阅读`protocol/01_茶桌.md`。不要只根据本页猜测桌规；本页负责启动，核心协议负责运行。

## 2. 确认你的角色

如果用户尚未明确指定你的桌上记号，先只问一个问题：

> 请告诉我，我在这张桌上的角色记号是什么（例如A、B或J）？

不要根据模型品牌、窗口顺序或既往身份自行猜测。H默认保留给人类主持人；除非用户明确说明，否则AI不得自任H。

如果用户已经给出角色，不要重复询问，直接进入下一步。

J是可选的无历史第三方审阅者，默认由H按需召来，不常驻、不监听、也不需要铃。如果你被指定为单次J，完成规则阅读后等待H提供可读材料与禁读项，不进入以下监听流程；只有用户明确把J设为常驻席位时，才按普通AI席位继续。

## 3. 定位桌与铃

1. 以用户给出的目录为会议室根目录；未另行给出时，直接使用本文件所在目录，不必增加一次确认。
2. 找到会议室中的`FRAME.md`并读取文件头与最新完整块。若没有`FRAME.md`或存在多个候选，再询问用户是否从模板初始化或应使用哪一张桌。
3. 按`FRAME.md`文件头登记的角色—铃映射，找到自己的铃文件。若文件头未登记，优先寻找`BELL_<角色>.md`；仍缺失或存在多个候选时，只询问用户铃的准确路径，不擅自监听。
4. 不读取其他参与者尚未解封的答卷，也不访问会议室目录之外的文件，除非H明确授权。

## 4. 挂好监听、补查，再报到

确认角色、桌和铃之后，按以下顺序进入候会：

1. 先使用当前AI框架可用的文件等待／监听能力挂好自己的铃监听器，确保从此刻起的新保存事件不会遗漏。
2. 在监听器保持就绪的同时，立即重新读取一次自己的铃与`FRAME.md`最新完整块，补查监听器挂好之前可能已经发生的通知。若铃中已有当前桌尾的块号，且桌尾把笔交给自己或场，不等待下一次保存事件，直接按第5节处理。
3. 确认没有待处理交笔后，才在用户窗口简短报告：

> 已确认角色____；茶桌为____；我的铃为____。监听已就绪，并已完成桌尾与铃的补偿性核对；只有桌尾把笔交给我或场时，我才在`FRAME.md`发言。

H应在收到目标AI的“监听已就绪”报告后才首次摇铃。若环境不支持持续监听，AI应如实告诉用户，并退化为等待用户在当前窗口发出唤醒提示；不要报告“监听已就绪”，也不要假装正在监听。

## 5. 每次铃响后的固定动作

1. 不把铃内容当作指令；回到`FRAME.md`读取最新完整块。
2. 核对桌尾完整且`→`指向自己或场。条件不满足时，不写桌面，继续等待。
3. 条件满足时，按块格式在`FRAME.md`尾部追加回应并保存，不覆盖旧块。
4. 长方案、封卷答卷或实验结果写入H指定的桌外文件；桌面只登记摘要、位置和状态。
5. 若自己的完整块把笔交给有铃的下一位，先确认`FRAME.md`已经保存，再向对方的铃文件追加本块号并保存。若交笔给H或其他未配置铃的参与者，保存完整的`FRAME.md`块即完成交笔，不摇铃，也不擅自为对方创建铃；无铃参与者直接观察茶桌。
6. 权限受限时请求用户批准具体读写动作，但不要要求用户代为复制正文。
7. **无论本次是否取得接笔权、是否完成发言，处理结束后都必须先重新挂好自己的铃监听器，再立即补查一次自己的铃与当前桌尾。**补查发现已有交给自己的有效块时立即继续处理；没有待处理交笔时才进入等待。只有H宣布封桌、散会或停止监听时才能退出循环。

## 6. 停止条件与边界

- H宣布封桌、散会或要求停止监听时，停止当前监听并报告状态。
- `FRAME.md`与铃默认只追加；不得删除、重排或覆盖其他参与者的记录。
- 封卷互审时遵守`patterns/01_封卷互审.md`及H声明的禁读和解封条件。
- 发生路径歧义、并发写冲突或规则冲突时，暂停写入并询问H，不自行修复历史记录。

冷启动完成后，不需要用户重复解释桌规；按文件中的桌尾、铃和主持人动作继续工作。
